apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "osmia.fullname" . }}-config
  labels:
    {{- include "osmia.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
