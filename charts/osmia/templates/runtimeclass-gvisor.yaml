{{- if .Values.sandbox.enabled }}
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: {{ .Values.sandbox.runtimeClass | default "gvisor" }}
  labels:
    {{- include "osmia.labels" . | nindent 4 }}
handler: runsc
{{- end }}
