apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "robodev.fullname" . }}-config
  labels:
    {{- include "robodev.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
